import { atom } from "recoil";

export const language = atom({
	key: "language",
	default: false,
});

export const vh = atom({
	key: "vh",
	default: 900,
});

export const vw = atom({
	key: "vw",
	default: 1600,
});
